<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error 404 - Página no encontrada</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="/public/error-page.css">
    <link rel="shortcut icon" href="/ico/photoVoyager-ico-generate.ico" type="image/x-icon">
</head>
<body>
    <div class="container">
        <div class="content">
            <h1 id="error-code">ERROR</h1>
            <p id="error-text">¡Ups! parece que hay un error</p>
            <a href="/" class="btn">Volver a la página de inicio</a>
            <div id="tooltip" class="tooltip">¡Ups! Aquí están los tipos de errores posibles
                <h4>1) Imagen no seleccionada</h4>
                <h4>2) Formato de imagen no válido</h4>
                <h4>3) No tienes permiso para ir a esta página</h4>
                <h4>4) Nombre de usuario o contraseña no válidos</h4>
                <h4>5) Nombre de usuario ya en uso</h4>
                <h4>6) Error de conexión</h4>
                </h4>
            </div>
        </div>
    </div>
    <script>
    // Esta parte es opcional, solo si quieres que la animación se reproduzca cuando se cargue la página
        window.addEventListener('load', () => {
            const text = document.getElementById('error-text');
            text.style.animation = 'none'; // Se detiene la animación
            text.offsetHeight; // Se fuerza un reflow
            text.style.animation = 'typing 3s steps(40, end)'; // Se reinicia la animación
        });
        
        const tooltip = document.getElementById('tooltip');
        const errorCode = document.getElementById('error-code');
        
        errorCode.addEventListener('mouseover', () => {
            tooltip.style.display = 'block';
        });
        
        errorCode.addEventListener('mouseout', () => {
            tooltip.style.display = 'none';
        });
        </script>
</body>
</html>s
