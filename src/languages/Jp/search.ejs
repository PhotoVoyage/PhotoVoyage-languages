<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Photo Gallery - 検索 Result</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
    <link rel="stylesheet" href="/public/admin.css">
    <link rel="shortcut icon" href="/ico/photoVoyager-ico-generate.ico" type="image/x-icon">
</head>
<body>
    <section class="section">
        <div class="container has-text-centered">
            <h1 class="title">検索 Result</h1>
            <div class="columns is-centered">
                <div class="column is-half">
                    <ul>
                        <li>
                            <div class="card">
                                <div class="card-image">
                                    <figure class="image is-4by3">
                                        <img src="/uploads/<%= photo.filename %>" alt="Photo" id="photo_<%= photo.id %>">
                                    </figure>
                                </div>
                                <div class="card-content">
                                    <div class="content">
                                        <p><strong>ファイル名：</strong> <%= photo.filename %></p>
                                        <p><strong>ID：</strong> <%= photo.id %></p>
                                    </div>
                                </div>
                                <footer class="card-footer">
                                    <form action="/admin/delete/<%= photo.id %>" method="post" onsubmit="return confirm('Are you sure you want to delete this photo? This action cannot be undone.');">
                                        <button type="submit" class="button is-danger card-footer-item">削除</button>
                                    </form>
                                </footer>
                            </div>
                        </li>
                    </ul>
                    <a href="/admin" class="button is-primary">管理画面に戻る</a>
                </div>
            </div>
        </div>
    </section>
</body>
</html>
